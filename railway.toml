[build]
builder = "nixpacks"

[deploy]
startCommand = "php artisan serve --host=0.0.0.0 --port=$PORT && php artisan migrate --force && php artisan db:seed --class=AdminSeeder --force"
healthcheckPath = "/api/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[deploy.variables]
PHP_VERSION = "8.2"
APP_ENV = "production"
APP_DEBUG = "false"